group "${group}.junit.core"
description = "JUnit Core"

dependencies {
    api project(":core")
    api libs.bundles.junit
    api group: "io.qameta.allure", name: "allure-junit5", version: "2.18.1"
}